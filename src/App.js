import React from 'react';
import { TypeInput } from './TypeInput';

function App() {
    return (
        <main
            style={{
                display: 'flex',
                justifyContent: 'center',
                alignItems: 'center',
                height: '100vh',
            }}
        >
            <TypeInput />
        </main>
    );
}

export default App;
